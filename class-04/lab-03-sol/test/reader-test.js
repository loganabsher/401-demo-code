'use strict';

const expect = require('chai').expect;
const fil...

decrible('Read...', ()=>{
  decrible('..'()=>{
    it('should error'()=>{
      fil(pth, (err)=>{
        expect(!err).to.equal(true);
      });
    });
  });
});
